package components

templ Title(text string) {
	@titleStyles()
	<h1 class="title">{ text }</h1>
}

templ titleStyles() {
	<style>
		.title {
			font-size: 70px;
			font-weight: bold;
			color: var(--color-white);
			margin-bottom: 12px;
			margin-top: 0;
		}
	</style>
}
